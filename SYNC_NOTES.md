# Runtime Discovery branch synchronization

This temporary branch exists only to merge the current `main` lineage into `bridge/runtime-discovery-v1` without force-rewriting history. It should not become part of the final feature surface.
